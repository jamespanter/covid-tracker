import React from "react";
import styles from "./Chart.module.scss";

const Chart = (props) => {
  const { data } = props;

  return (
    <>
      <p>Chart works</p>
    </>
  );
};

export default Chart;
